services:
  nginx:
    build:
      context: .
      dockerfile: react-vite/Dockerfile
    ports:
      - 80:80
    networks:
      - internal

  php:
    build:
      context: .
      dockerfile: laravel/Dockerfile
    networks:
      - internal

networks:
  internal:
    driver: bridge
